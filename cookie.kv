<Cookie>:
    canvas.before:
        Color:
            rgb: (0.7, 0.7, 0.7)
        Rectangle:
            size: self.width, self.height
            pos: self.x, self.y

    Widget:
        size_hint: 0.47, 0.8
        pos_hint: {'x': 0.02, 'y': 0.02}
        canvas:
            Color:
                rgb: (0, 1, 0)
            Rectangle:
                size: self.width, self.height
                pos: self.x, self.y
    Widget:
        size_hint: 0.47, 0.8
        pos_hint: {'x': 0.51, 'y': 0.02}
        canvas:
            Color:
                rgb: (1, 1, 0)
            Rectangle:
                size: self.width, self.height
                pos: self.x, self.y

    BoxLayout:
        size_hint: 0.47, 0.14
        pos_hint: {'x': 0.02, 'y': 0.84}
        spacing: 5
        padding: 5,5,5,5
        canvas:
            Color:
                rgb: (1, 1, 1)
            Rectangle:
                size: self.width, self.height
                pos: self.x, self.y
        Widget:
            canvas:
                Color:
                    rgb: (1, 0, 0)
                Rectangle:
                    size: self.width, self.height
                    pos: self.x, self.y
        Widget:
            canvas:
                Color:
                    rgb: (1, 0, 0)
                Rectangle:
                    size: self.width, self.height
                    pos: self.x, self.y
        Widget:
            canvas:
                Color:
                    rgb: (1, 0, 0)
                Rectangle:
                    size: self.width, self.height
                    pos: self.x, self.y
        Widget:
            canvas:
                Color:
                    rgb: (1, 0, 0)
                Rectangle:
                    size: self.width, self.height
                    pos: self.x, self.y

    BoxLayout:
        size_hint: 0.47, 0.14
        pos_hint: {'x': 0.51, 'y': 0.84}
        spacing: 5
        padding: 5,5,5,5
        canvas:
            Color:
                rgb: (1, 1, 1)
            Rectangle:
                size: self.width, self.height
                pos: self.x, self.y
        Widget:
            canvas:
                Color:
                    rgb: (0, 0, 1)
                Rectangle:
                    size: self.width, self.height
                    pos: self.x, self.y
        Widget:
            canvas:
                Color:
                    rgb: (0, 0, 1)
                Rectangle:
                    size: self.width, self.height
                    pos: self.x, self.y
        Widget:
            canvas:
                Color:
                    rgb: (0, 0, 1)
                Rectangle:
                    size: self.width, self.height
                    pos: self.x, self.y
        Widget:
            canvas:
                Color:
                    rgb: (0, 0, 1)
                Rectangle:
                    size: self.width, self.height
                    pos: self.x, self.y