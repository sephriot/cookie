<Picture>:
    size_hint: None, None
    size: image.size[0] * self.scale, image.size[1] * self.scale
    do_translation: None

    Image:
        id: image
        source: root.source
        size: self.texture_size